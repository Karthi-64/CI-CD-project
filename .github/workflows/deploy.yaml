name: Simulated Multi-Environment CI/CD

on:
  push:
    branches:
      - dev
      - staging
      - main

jobs:
  simulate-deployment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Identify environment
        run: echo "Deploying to environment:${{ github.ref_name }}"

      - name: Simulate build step
        run: |
          echo "🛠️ Building project..."
          sleep 2
          echo "✅ Build complete."

      - name: Simulate deploy step
        run: |
          echo "🚀 Deploying application to ${{ github.ref_name }} environment..."
          echo "Applying configuration from k8s/${{ github.ref_name }}/deployment.yaml"
          echo "----- CONFIG CONTENT -----"
          cat k8s/${{ github.ref_name }}/deployment.yaml
          echo "--------------------------"
          echo "🎉 Deployment simulated successfully!"
